# DQIII8 — Environment Configuration
# Copy this file to config/.env and fill in your values.
# Never commit config/.env to version control.

# ── Required for Tier B (free cloud) ─────────────────────────────────
GROQ_API_KEY=           # Get from console.groq.com (free)
# TOGETHER_API_KEY=     # Optional alternative to Groq: together.ai (free tier)

# ── Required for Tier A (paid) — optional, system works without it ────
# ANTHROPIC_API_KEY=    # Get from console.anthropic.com

# ── Optional: additional providers ───────────────────────────────────
# OPENAI_API_KEY=       # OpenAI (gpt-4o, gpt-4o-mini)
# OPENROUTER_API_KEY=   # OpenRouter — access many models via one key

# ── Telegram bot — optional feature ──────────────────────────────────
# TELEGRAM_BOT_TOKEN=   # Get from @BotFather on Telegram
# TELEGRAM_CHAT_ID=     # Your Telegram chat ID (send /start to your bot)

# ── Ollama ────────────────────────────────────────────────────────────
OLLAMA_BASE_URL=http://localhost:11434

# ── Optional integrations ─────────────────────────────────────────────
# GITHUB_TOKEN=         # For github_researcher.py (5000 req/h vs 60 without)
# GEMINI_API_KEY=       # For /gemini-review workflow

# ── Infrastructure (used by bin/ scripts) ────────────────────────────
# CONTACT_EMAIL=        # Contact email for OpenAlex API polite pool (knowledge_harvester.py)
# DQIII8_VPS_HOST=      # SSH target for VPS scripts, e.g. root@your-vps-ip (chrome_connect_local.sh)

# ── MCP server portability (.mcp.json) ───────────────────────────────
# Required for filesystem, fetch, dqiii8-db, and pal MCP servers.
# Set these in your shell profile (e.g., ~/.bashrc or Windows System Env Vars).
# DQIII8_ROOT=/root/dqiii8          # VPS: /root/dqiii8 | Windows: C:\DQIII8\dqiii8
# PYTHON=python3                    # VPS: python3       | Windows: python
# PAL_SERVER_PATH=/tmp/pal-mcp-server/server.py  # VPS only — skip on Windows
