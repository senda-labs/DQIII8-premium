# /test-team — Agent Team Coordination Test

> **SSOT: `.claude/skills/test-team/SKILL.md`.** This command file is a pointer
> only — read the skill for the two-agent brief, the implementation
> requirements and the `tasks/results/` handoff protocol. It previously carried
> a duplicate copy that held content the skill had dropped: the Kelly
> implementation requirements (type hints, input-validation bounds, Half-Kelly
> default, docstring example) and the sequencing contract ("the orchestrator
> waits for the result from research-analyst before passing context to
> python-specialist"). Both were merged into the skill on 2026-08-18 (F2).
