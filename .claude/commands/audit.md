# /audit — System Health Audit

> **SSOT: `.claude/skills/audit/SKILL.md`.** This command file is a pointer
> only — read the skill for the full procedure, invocation frontmatter, and
> auto-invoke trigger.
