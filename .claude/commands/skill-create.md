# /skill-create — Skill Generation from Git History

> **SSOT: `.claude/skills/skill-create/SKILL.md`.** This command file is a
> pointer only — read the skill for the full procedure and invocation
> frontmatter. It previously carried a second, much longer copy of the
> analysis steps (git-log parsing, pattern table, INDEX.md update) that had
> forked from the skill (0.06 prose similarity) — reconciling which of the
> two was current was not attempted; the skill is the one the runtime
> actually invokes.
